# notepad-react
notepad written in react and typescript

## Getting started

Install packages:
`npm install`

Build project:
`npm run-script build`

Start project:
`npm start`

Refresh changes:
`npm run-script watch`
